import { h } from 'preact';
import { UIPlugin, Uppy } from '@uppy/core';
import { type CompanionPluginOptions } from '@uppy/companion-client';
import type { Body, Meta } from '@uppy/utils/lib/UppyFile';
import type { AsyncStore, BaseProviderPlugin } from '@uppy/core/lib/Uppy.js';
export type GooglePhotosPickerOptions = CompanionPluginOptions & {
    clientId: string;
};
export default class GooglePhotosPicker<M extends Meta & {
    width: number;
    height: number;
}, B extends Body> extends UIPlugin<GooglePhotosPickerOptions, M, B> implements BaseProviderPlugin {
    static VERSION: any;
    static requestClientId: string;
    type: string;
    icon: () => h.JSX.Element;
    storage: AsyncStore;
    defaultLocale: {
        strings: {};
    };
    constructor(uppy: Uppy<M, B>, opts: GooglePhotosPickerOptions);
    install(): void;
    uninstall(): void;
    private handleFilesPicked;
    render: () => h.JSX.Element;
}
//# sourceMappingURL=GooglePhotosPicker.d.ts.map